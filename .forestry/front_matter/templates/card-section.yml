---
label: Card Section
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: cards
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: card-grid
  label: Cards
