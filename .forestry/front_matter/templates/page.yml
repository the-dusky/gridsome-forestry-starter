---
label: Page
hide_body: true
fields:
- name: name
  type: blocks
  label: Name
  template_types:
  - journal
  - theme-configuration
  - project
  - author
  config:
    min: 
    max: 
- name: route
  type: text
  config:
    required: false
  label: Route
