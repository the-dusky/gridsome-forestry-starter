---
label: Page
hide_body: true
fields:
- name: name
  type: text
  config:
    required: false
  label: Name
- name: route
  type: text
  config:
    required: false
  label: Route
- name: components
  type: blocks
  label: Components
  template_types:
  - card_component
  - cards_component
  config:
    min: 
    max: 
